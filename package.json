{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "## Folders",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel src/*.html --open",
    "build": "parcel build src/*.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.15.2"
  },
  "browserslist": [
    "Chrome >= 63",
    "Firefox >= 63",
    "Safari >= 10.1",
    "Edge >= 79"
  ]
}
